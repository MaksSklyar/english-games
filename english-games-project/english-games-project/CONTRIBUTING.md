# 🤝 Contributing Guide

Дякуємо за інтерес до проекту! Ось як ви можете допомогти.

## 🚀 Швидкий старт

1. **Клонуйте репозиторій**
   ```bash
   git clone <repository-url>
   cd english-games-project
   ```

2. **Відкрийте у VS Code**
   ```bash
   code .
   ```

3. **Встановіть рекомендовані розширення**
   - Live Server (для локального сервера)
   - Prettier (для форматування коду)
   - Auto Rename Tag (для HTML)

4. **Запустіть проект**
   - Використайте Live Server: Клік правою кнопкою на `index.html` → "Open with Live Server"
   - Або відкрийте `index.html` прямо в браузері

## 📁 Структура файлів

```
english-games-project/
├── index.html              # Головна сторінка
├── css/                    # Стилі
│   ├── styles.css         # Основні стилі
│   ├── animations.css     # Анімації
│   └── games.css          # Стилі ігор
├── js/                     # JavaScript
│   ├── config.js          # Конфігурація
│   ├── utils.js           # Допоміжні функції
│   ├── gameManager.js     # Менеджер ігор
│   └── main.js            # Точка входу
└── games/                  # Ігри
    ├── game01.js          # Гра 1
    ├── game02.js          # Гра 2
    └── ...
```

## 🎮 Як додати нову гру

### Крок 1: Створіть файл гри

Створіть `gameXX.js` у папці `games/`:

```javascript
// Game XX: Назва гри
const GameXX = {
    // Ініціалізація (опціонально)
    init() {
        this.score = 0;
        this.attempts = 0;
    },

    // Рендер HTML
    render() {
        return `
            <h2>Game XX: Назва Гри</h2>
            <h3>Переклад</h3>
            <div class="instruction">Опис гри...</div>
            <div class="game-area">
                <!-- Вміст гри -->
            </div>
            <button class="complete-btn" 
                    onclick="GameManager.completeGame(XX, STARS)">
                Завершити! ⭐
            </button>
        `;
    },

    // Методи гри
    checkAnswer(answer) {
        if (answer === 'correct') {
            Utils.createConfetti();
            Utils.playSound('Correct!');
            setTimeout(() => {
                GameManager.completeGame(XX, STARS);
            }, 1000);
        } else {
            Utils.showMessage('Try again!');
        }
    }
};
```

### Крок 2: Додайте конфігурацію

У файлі `js/config.js` додайте:

```javascript
{
    number: XX,
    title: "Game Title",
    subtitle: "Переклад",
    description: "Опис гри...",
    stars: 5,  // Кількість зірок
    icon: "🎮"  // Емодзі іконка
}
```

### Крок 3: Підключіть скрипт

У `index.html` додайте перед `main.js`:

```html
<script src="games/gameXX.js"></script>
```

### Крок 4: Протестуйте

1. Відкрийте гру в браузері
2. Перевірте всі функції
3. Переконайтесь, що звук працює
4. Протестуйте на різних екранах

## 🎨 Стилістичні правила

### CSS

```css
/* Використовуйте CSS змінні */
.my-element {
    background: var(--pink);
    color: var(--dark);
}

/* Називайте класи описово */
.game-card { } /* Добре */
.gc { }        /* Погано */

/* Групуйте пов'язані стилі */
.button {
    /* Позиціонування */
    position: relative;
    
    /* Розміри */
    width: 100%;
    padding: 15px;
    
    /* Візуальні властивості */
    background: var(--green);
    border-radius: 15px;
    
    /* Текст */
    font-size: 1.2rem;
    color: white;
    
    /* Анімації */
    transition: all 0.3s ease;
}
```

### JavaScript

```javascript
// Використовуйте const/let замість var
const MAX_ATTEMPTS = 3;
let currentScore = 0;

// Функції з описовими назвами
function checkUserAnswer() { }  // Добре
function check() { }            // Погано

// Коментарі для складної логіки
// Перевіряємо чи відповідь правильна
if (answer === correctAnswer) {
    // Показуємо конфетті
    Utils.createConfetti();
}

// Використовуйте template literals
const message = `You scored ${score} points!`;
```

## 🐛 Повідомлення про помилки

Якщо знайшли баг:

1. Перевірте, чи існує вже issue
2. Створіть новий issue з:
   - Чіткою назвою
   - Кроками для відтворення
   - Очікуваним результатом
   - Фактичним результатом
   - Скріншотами (якщо потрібно)

## ✨ Додавання функцій

Перед додаванням нової функції:

1. Створіть issue з описом
2. Дочекайтесь обговорення
3. Створіть branch
4. Зробіть зміни
5. Створіть Pull Request

## 📝 Код-стайл

- Відступи: 4 пробіли
- Крапка з комою: завжди
- Лапки: одинарні для JavaScript, подвійні для HTML
- Максимальна довжина рядка: 100 символів

## 🧪 Тестування

Перед створенням PR:

1. ✅ Код працює у Chrome, Firefox, Safari
2. ✅ Responsive дизайн працює на мобільних
3. ✅ Немає помилок у консолі
4. ✅ Всі анімації працюють плавно
5. ✅ Озвучка працює коректно

## 📱 Підтримка браузерів

- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

## 🌍 Локалізація

Якщо додаєте текст:

```javascript
// Зберігайте текст окремо для майбутньої локалізації
const MESSAGES = {
    success: 'Great job!',
    tryAgain: 'Try again!',
    complete: 'Game completed!'
};
```

## 💡 Поради

- Використовуйте Console.log для дебагу
- Тестуйте на реальних дітях (якщо можливо)
- Робіть гри веселими та освітніми
- Додавайте звуки та анімації
- Підтримуйте простоту

## 📬 Контакт

Якщо питання - створіть issue або напишіть нам!

---

Дякуємо за ваш внесок! 🎉
